# Banco de dados
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USERNAME:paroquia}
quarkus.datasource.password=${DB_PASSWORD:paroquia123}
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/paroquia}

# Flyway
quarkus.flyway.migrate-at-start=true

# Hibernate
quarkus.hibernate-orm.schema-management.strategy=validate
quarkus.hibernate-orm.log.sql=true

# Swagger UI sempre visível
quarkus.swagger-ui.always-include=true

# Keycloak / OIDC
quarkus.oidc.auth-server-url=http://localhost:8081/realms/paroquia-em-rede
quarkus.oidc.client-id=paroquia-backend
quarkus.oidc.credentials.secret=yzF8L6AyJ8ua6yGfkAshJDvhE6I3LX55
quarkus.oidc.application-type=service

# Porta do Quarkus (explicita porque o Keycloak ocupa a 8081 agora)
quarkus.http.port=8080